"""
Общая блокировка для задач Ozon API: парсер очереди поставок и отслеживатель слотов
не должны выполняться одновременно (лимиты запросов в секунду).
Приоритет у парсера: если отслеживатель запланирован в тот же момент — он откладывается.
"""
from __future__ import annotations

import asyncio
from loguru import logger

# Один запуск за раз (парсер или отслеживатель)
ozon_runner_lock = asyncio.Lock()

# На сколько секунд откладывать повторный запуск отслеживателя, если занят парсер
TRACKER_DEFER_SECONDS = 600  # 10 минут
